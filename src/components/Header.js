import React from 'react';
function Header(){
    return (
        <footer>
            <h2>This is Header! Date : {new Date().toLocaleTimeString()}</h2>
        </footer>
      );
}
export default Header;